name: user_onboarding
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.2
  
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.5 
  intl: ^0.18.1
  shared_preferences: ^2.2.2
  # PostgreSQL dependencies
  postgres: ^2.6.1
  dotenv: ^4.1.0
  path_provider: ^2.0.15
  uuid: ^4.0.0
  connectivity_plus: ^4.0.1
  
  # Use fl_chart instead of charts_flutter to avoid dependency conflicts
  fl_chart: ^0.65.0

flutter:
  uses-material-design: true
  
  assets:
    - .env